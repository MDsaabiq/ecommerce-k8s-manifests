apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: default
spec:
  # ClusterIP — only reachable inside the cluster
  # Ingress routes /api/* traffic here
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
